import { ClientWithReservationResponse } from './client-with-reservation-response';

describe('ClientWithReservationResponse', () => {
  it('should create an instance', () => {
    expect(new ClientWithReservationResponse()).toBeTruthy();
  });
});
